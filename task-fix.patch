diff --cc App.js.html
index 7449b16,137d6a0..0000000
--- a/App.js.html
+++ b/App.js.html
@@@ -145,32 -145,16 +145,45 @@@
          };
  
          google.script.run
++<<<<<<< ours
 +<<<<<<< ours
 +          .withSuccessHandler((response) => {
 +            creatingTask.value = false;
 +
 +            const result = typeof response === 'string' ? JSON.parse(response) : response;
 +            if (!result || !result.success) {
 +              alert(result?.error || 'Failed to create task.');
 +              return;
 +            }
 +
 +            if (result.task) {
 +              tasks.value.push(result.task);
 +            } else {
 +              fetchData();
 +=======
 +          .withSuccessHandler((result) => {
 +            const parsedResult = typeof result === 'string' ? JSON.parse(result) : result;
 +            if (!parsedResult || !parsedResult.success) {
 +              alert(parsedResult?.error || 'Failed to create task.');
 +              return;
 +            }
 +
 +            tasks.value.push(parsedResult.task);
 +            if (Array.isArray(parsedResult.assignments) && parsedResult.assignments.length > 0) {
++              assignments.value.push(...parsedResult.assignments);
++>>>>>>> theirs
++=======
+           .withSuccessHandler((result) => {
+             const parsedResult = typeof result === 'string' ? JSON.parse(result) : result;
+             if (!parsedResult || !parsedResult.success) {
+               alert((parsedResult && parsedResult.error) || 'Failed to create task.');
+               return;
+             }
+ 
+             tasks.value.push(parsedResult.task);
+             if (Array.isArray(parsedResult.assignments) && parsedResult.assignments.length > 0) {
                assignments.value.push(...parsedResult.assignments);
 +>>>>>>> theirs
              }
              closeAddTask();
            })
diff --cc Index.html
index 43ede3c,1275025..0000000
--- a/Index.html
+++ b/Index.html
@@@ -114,10 -115,10 +115,17 @@@
  
  
  
++<<<<<<< ours
 +    <div v-if="addTaskModalOpen" class="fixed inset-0 bg-black/40 z-50 flex items-center justify-center p-4">
 +      <div class="bg-white rounded-2xl shadow-xl w-full max-w-2xl p-6">
 +        <div class="flex items-center justify-between mb-5">
 +          <h3 class="text-lg font-semibold text-gray-800">Add Task to {{ addTaskProject?.Project_Title }}</h3>
++=======
+     <div v-show="addTaskModalOpen" style="display: none;" class="fixed inset-0 bg-black/40 z-50 flex items-center justify-center p-4">
+       <div class="bg-white rounded-2xl shadow-xl w-full max-w-2xl p-6">
+         <div class="flex items-center justify-between mb-5">
+           <h3 class="text-lg font-semibold text-gray-800">Add Task to {{ addTaskProject ? addTaskProject.Project_Title : '' }}</h3>
++>>>>>>> theirs
            <button @click="closeAddTask" class="text-gray-400 hover:text-gray-600">
              <i class="fa-solid fa-xmark text-lg"></i>
            </button>
diff --git a/Code.js b/Code.js
index c787489..a42c86d 100644
--- a/Code.js
+++ b/Code.js
@@ -1,6 +1,8 @@
 /**
  * Main entry point for the web app.
  * Serves the Index.html file.
+ * 
+ * im not convinced the changes were made
  */
 function doGet() {
   return HtmlService.createTemplateFromFile('Index')
